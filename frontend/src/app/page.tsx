export default function HomePage() {
  return (
    <main className="home">
      <section className="hero">
        <p className="eyebrow">BookIt</p>
        <h1>Find your next live event.</h1>
        <p>
          The project foundation is ready. Event discovery, booking, and
          organizer tools will be added in the next milestones.
        </p>
      </section>
    </main>
  );
}
